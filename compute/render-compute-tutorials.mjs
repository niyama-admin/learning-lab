import fs from "node:fs";
import path from "node:path";

const root = path.dirname(new URL(import.meta.url).pathname.replace(/^\/(.:)/, "$1"));
const profiles = [
  {file:"tutorials/01-architecture-map.md",title:"The Map: ISA, Microarchitecture, Programming Model, and Product",sources:"Sun et al., CPU/GPU design trends; AMD64, Arm, CUDA, HIP, and oneAPI documentation",question:"When people say x86, Arm, CUDA, or GPU architecture, which layer are they actually describing?",analogy:"a language contract, a particular factory that implements it, a work-allocation system, and a purchasable factory configuration",flow:["source program","compiler and ABI","ISA instructions","microarchitecture","measured product"],core:"An ISA defines software-visible instructions, registers, exceptions, addressing, and memory rules. A microarchitecture chooses pipelines, caches, predictors, schedulers, and execution units. A programming model exposes parallel work and memory spaces. A product fixes counts, capacities, clocks, links, and power limits.",compare:"x86-64 and AArch64 are CPU ISAs. CUDA and HIP are programming models and software platforms, not GPU ISAs. NVIDIA SM, AMD CU, and Intel Xe-core are vendor implementation units whose internal details vary by generation.",formal:"Runtime T is a function of algorithm A, compiler C, ISA I, microarchitecture M, product configuration P, and system state S: T = f(A,C,I,M,P,S). Changing one label rarely holds the others fixed.",lab:"Compile the same scalar loop for x86-64 and AArch64, inspect assembly, and write a four-column map from source operation to ISA instruction, likely execution resource, and measured event."},
  {file:"tutorials/02-memory-hierarchy.md",title:"Memory from CPU Registers and Caches to GPU HBM",sources:"AMD64 and Arm memory documentation; CUDA/HIP guides; Mei and Chu GPU memory microbenchmarking",question:"Why can the same arithmetic run hundreds of times slower depending on where data resides and how it is accessed?",analogy:"a desk, nearby cabinet, building archive, and remote warehouse, with address forwarding and several clerks keeping copies consistent",flow:["virtual address","translation and TLB","cache hierarchy","DRAM or HBM","CPU-GPU interconnect"],core:"Registers are explicitly named fast storage. Caches automatically retain recently used cache lines. Virtual memory translates process addresses through page tables and TLBs. CPU DRAM and discrete-GPU HBM/GDDR are normally separate physical pools connected by PCIe or a coherent fabric. Unified memory gives a common address-space abstraction but may migrate pages or access them remotely.",compare:"CPUs invest in large coherent caches and latency reduction for a few complex threads. GPUs invest in bandwidth, many outstanding requests, coalescing, and latency hiding across many warps or wavefronts. Integrated systems may share physical memory but still have distinct caches and synchronization rules.",formal:"Effective time is not bytes/bandwidth alone: T = T_launch + T_transfer + T_translation + T_cache_miss + T_compute + T_sync. Arithmetic intensity I = operations/bytes helps determine whether a kernel is compute- or bandwidth-bound.",lab:"Measure array traversal with sequential, strided, and pointer-chasing patterns across working-set sizes. Plot latency and bandwidth, then identify likely cache, TLB, and DRAM regions without claiming exact geometry from one run."},
  {file:"tutorials/x86-64/03-x86-64.md",title:"x86-64: The ISA Contract and Modern CPU Execution",sources:"AMD64 Architecture Programmer's Manual; Intel Software Developer Manual; Hofmann et al. microarchitecture analysis",question:"How does a variable-length, compatibility-rich instruction set execute efficiently on modern superscalar processors?",analogy:"an old postal code system preserved at the street-facing counter while a modern sorting plant translates requests into its own internal jobs",flow:["x86-64 bytes","decode and micro-ops","rename and schedule","execution ports","retire in order"],core:"Long mode expands general-purpose registers and virtual addresses while retaining historical encodings. Implementations fetch variable-length instructions, decode many into internal micro-operations, rename architectural registers onto physical registers, execute ready work out of order, and retire in program order for precise state. SIMD extensions operate on multiple lanes.",compare:"The ISA guarantees outcomes, not port mappings, cache sizes, reorder-buffer depth, or instruction latency. AMD and Intel implement the same broad software contract with different predictors, cache topology, execution resources, and generation-specific extensions.",formal:"A dependency-limited loop is bounded by critical-path latency; independent operations are bounded by reciprocal throughput, issue width, ports, and front-end supply. Cycles per iteration is at least max(dependency bound, port-pressure bound, front-end bound, memory bound).",lab:"Compile a reduction and an element-wise transform at multiple optimization levels. Use disassembly to identify loads, vector instructions, and branches; use a profiler to test, rather than assume, the bottleneck."},
  {file:"tutorials/arm/04-arm-a-profile.md",title:"Arm A-profile: AArch64, Vector Execution, and Weak Memory",sources:"Armv8-A CPU Architecture; Neon Programmer Guide; Podkopaev et al. weak-memory paper",question:"What is common to Arm A-profile processors, and what remains an implementation choice?",analogy:"a concise rulebook licensed to many builders, each free to design a different engine as long as programs observe the specified results",flow:["A64 instruction","architectural registers","implementation pipeline","cache and coherency","observable order"],core:"AArch64 uses fixed 32-bit instruction encodings, thirty-one general-purpose registers, exception levels, and explicit load/store operations. Neon exposes 128-bit Advanced SIMD registers; SVE later adds vector-length-agnostic programming. A weakly ordered memory model permits more reordering than x86-TSO, so concurrent code uses acquire, release, and barriers.",compare:"Arm is an architecture family and licensing ecosystem, not one low-power core. Implementations range from small in-order cores to wide server CPUs. Reduced instruction encoding complexity does not by itself determine performance or energy; the microarchitecture, process, memory system, and workload do.",formal:"For concurrent events, program order is not identical to global observation order. Correctness is expressed through relations such as reads-from, coherence order, and happens-before. Acquire/release operations create edges that forbid executions a data-race-free algorithm cannot tolerate.",lab:"Cross-compile a loop to AArch64 and inspect scalar versus Neon output. Then model a message-passing litmus test and explain why an acquire load and release store are required instead of relying on source-code order."},
  {file:"tutorials/gpu/05-gpu-foundations.md",title:"GPU Foundations: Throughput, SIMT/SIMD, and Memory",sources:"Sun et al. CPU/GPU trends; Mei and Chu memory study; CUDA, HIP, and oneAPI guides",question:"Why are GPUs excellent for some workloads and disappointing for others?",analogy:"a large kitchen organized around many identical meals, compared with a master chef optimized for unpredictable custom orders",flow:["host program","kernel launch","thread groups","parallel execution units","device memory"],core:"A GPU devotes more area to replicated arithmetic and thread state than to sophisticated single-thread latency mechanisms. Thousands of logical threads are grouped for scheduling; groups share fast local memory and synchronize locally. Hardware swaps among ready groups to hide latency. Regular data-parallel work maps well; serial dependencies, irregular branches, tiny jobs, and random memory access do not.",compare:"A CPU usually has fewer powerful cores, deep caches, branch prediction, speculative out-of-order execution, and strong latency performance. A GPU has many narrower lanes, high memory bandwidth, and massive concurrency. Neither is universally faster; parallel fraction, arithmetic intensity, transfer cost, and library quality decide.",formal:"A roofline upper bound is performance <= min(peak_compute, memory_bandwidth * arithmetic_intensity). End-to-end speedup must also include serial fraction and offload overhead; Amdahl's law bounds speedup by 1/((1-p)+p/s).",lab:"Implement vector addition and a reduction in a portable framework. Vary problem size and include allocation, transfer, launch, synchronization, and verification in separate timers."},
  {file:"tutorials/gpu/nvidia/06-cuda-architecture.md",title:"NVIDIA GPUs through the CUDA Programming Guide",sources:"NVIDIA CUDA Programming Guide Release 13.x",question:"How do CUDA's grid, block, warp, and memory abstractions map to NVIDIA hardware?",analogy:"a dispatcher sends independent crews to workshops; workers in each crew move in squads and share one workbench",flow:["CPU host","kernel grid","thread blocks","warps on SM","global HBM/GDDR"],core:"A kernel launch creates a grid of blocks. A block is scheduled onto one streaming multiprocessor and is divided into 32-thread warps. Threads have registers; a block has shared memory; all threads can access global memory. Occupancy depends on registers, shared memory, threads, and architectural limits. Coalesced accesses combine nearby lane requests; divergence serializes paths within a warp.",compare:"CUDA specifies a programming model across NVIDIA generations. Compute capability identifies feature sets and limits, while exact cache geometry and instruction behavior may differ. Unified memory is managed accessibility, not a promise that CPU and GPU access the same physical location at equal cost.",formal:"For a block using R registers/thread, S bytes shared/block, and B threads, resident blocks are bounded by min(register_file/(RB), shared_capacity/S, thread_limit/B, block_limit). The tightest resource determines occupancy.",lab:"Write tiled matrix multiplication after a correct naive version. Measure global-load efficiency, occupancy, and time; change tile size one variable at a time and verify numerical error."},
  {file:"tutorials/gpu/nvidia/07-nvidia-microbenchmarking.md",title:"NVIDIA Microbenchmarking: From Specification to Evidence",sources:"Jia et al. Volta study; Mei and Chu memory-hierarchy study",question:"How can researchers infer undocumented latency, bandwidth, cache, and scheduler properties without confusing artifacts for architecture?",analogy:"learning a sealed building's layout by sending timed messengers along carefully chosen routes",flow:["hypothesis","minimal kernel","controlled perturbation","timing/counters","competing explanation"],core:"Pointer chasing exposes dependent-load latency; stride sweeps reveal capacity and line effects; throughput kernels create independent requests; instruction chains estimate latency; independent instructions estimate throughput. Generated machine code, compiler motion, clocks, warming, residency, and measurement overhead must be controlled.",compare:"Vendor guides define supported semantics and limits but not every physical detail. Microbenchmarks add empirical implementation knowledge. Results for Volta, Maxwell, or a particular driver are not automatically true for Ampere, Hopper, Blackwell, or virtualized cloud GPUs.",formal:"Observed cycles O = target latency L + measurement overhead H + interference E. A valid design estimates H, bounds E through repetitions and controls, and varies one parameter whose predicted response differs between hypotheses.",lab:"Design a pointer-chasing kernel whose next address depends on the current load. Inspect generated code, subtract timer overhead, sweep working-set size, report distributions, and label cache-boundary conclusions as inferred."},
  {file:"tutorials/gpu/amd/08-amd-cdna-hip.md",title:"AMD CDNA and HIP: Compute Units, Wavefronts, LDS, and HBM",sources:"AMD CDNA whitepaper; AMD ROCm HIP Programming Guide",question:"How does AMD's compute-focused GPU stack express the same broad parallel ideas with different architectural names and choices?",analogy:"another large workshop system whose crews are called wavefronts, whose workbench is LDS, and whose factories connect through Infinity Fabric",flow:["HIP host","grid and work-groups","wavefronts","compute units/matrix cores","HBM and fabric"],core:"HIP launches kernels over grids and blocks/work-groups. AMD ISA groups lanes into wavefronts; compute units contain vector/scalar resources and Local Data Share. CDNA targets HPC and AI, adds matrix-core engines, HBM bandwidth, and high-speed GPU links. HIP source resembles CUDA but performance portability still requires vendor-aware tuning.",compare:"NVIDIA warp and AMD wavefront play analogous scheduling roles but widths and detailed execution differ by architecture. CUDA shared memory and AMD LDS are analogous block-local storage. Names and source portability do not make occupancy, coalescing, cache, matrix instructions, or collective behavior identical.",formal:"Useful throughput is bounded by issue resources, active wavefronts, LDS/register allocation, memory bandwidth, and interconnect traffic. Multi-GPU scaling efficiency E = T1/(N*Tn) falls when communication or imbalance grows.",lab:"Port vector addition and a tiled transpose through HIP. Record the compiled target architecture, wavefront assumptions, LDS usage, host-device transfer, and kernel timing; never compare vendor peaks without equal precision and scope."},
  {file:"tutorials/gpu/intel/09-intel-xe-oneapi.md",title:"Intel Xe and oneAPI: Xe-cores, SIMD, SLM, XMX, and SYCL",sources:"Intel Xe-HPG whitepaper; Intel oneAPI GPU Optimization Guide",question:"How does Intel expose GPU parallelism from integrated graphics through discrete and HPC products?",analogy:"a modular factory built from slices and Xe-cores, programmed through a portable dispatch language but containing vendor-specific vector and matrix machinery",flow:["SYCL host","ND-range","work-groups/subgroups","Xe-core XVE/XMX","SLM caches and device memory"],core:"Xe is a family, not one fixed GPU. Xe-HPG organizes slices, Xe-cores, vector engines, matrix engines, shared local memory, caches, and fixed-function graphics units. SYCL/oneAPI expresses devices, queues, kernels, work-groups, subgroups, and memory. XMX systolic operations accelerate lower-precision matrix work.",compare:"A SYCL subgroup resembles a CUDA warp or HIP wavefront as a cooperative execution group, but size can vary and must be queried or constrained carefully. SLM is block/work-group-local storage. Integrated Xe may share system DRAM physically; discrete Xe uses local device memory and a host link.",formal:"Peak matrix operations assume supported types, full XMX utilization, and counting conventions such as multiply-add as two operations. Attainable performance is reduced by layout conversion, occupancy, bandwidth, dependencies, and incomplete tiles.",lab:"Express vector addition and a tiled matrix operation in SYCL. Query device and subgroup properties, collect oneAPI profiling timestamps, and compare buffer/USM choices without assuming shared addressing means uniform access cost."},
  {file:"tutorials/10-cpu-gpu-decision.md",title:"Choosing CPU, GPU, and Memory Placement",sources:"CPU/GPU trend paper; vendor programming guides; roofline and Amdahl performance models",question:"Given a real workload, when should it stay on the CPU, move to a GPU, or be split?",analogy:"deciding whether to do a job in a nearby flexible workshop or ship a large batch to a distant mass-production plant",flow:["workload graph","parallelism and locality","cost model","prototype and profile","placement decision"],core:"Start from operations, dependencies, bytes moved, batch size, latency objective, and existing libraries. CPUs suit branchy control, small requests, operating-system work, and low-latency serial sections. GPUs suit large regular batches with high parallelism or optimized dense/sparse primitives. Hybrid designs keep orchestration on CPU and kernels/data near the GPU.",compare:"Peak FLOPS is not application performance. Include PCIe/fabric transfer, page migration, kernel launch, queueing, synchronization, preprocessing, framework overhead, and CPU work. Integrated memory may reduce copies but not erase bandwidth contention or cache ownership.",formal:"Choose GPU when T_gpu = T_setup + T_H2D + T_kernel + T_D2H + T_sync is below T_cpu at the required tail latency and cost. For repeated work, amortize setup and retain resident data; for pipelines, overlap transfer and compute only when dependencies permit.",lab:"Build a decision memo for one LLM inference or matrix/data-processing workload. Measure CPU baseline, optimized library baseline, GPU steady state, and true end-to-end path; report break-even batch size and sensitivity to data residence."}
];

const prerequisites = `## Appendix - Prerequisites

### Prerequisite 1 - Binary, instructions, and state

Bits acquire meaning through a contract. The same 64-bit pattern may be an integer, floating-point value, address, or instruction encoding. An instruction names an operation and operands; architectural state includes registers, memory, status, and control state visible to software. Endianness orders bytes in multi-byte values but does not reverse bits inside every byte. Practice by encoding 13 as unsigned binary, signed two's complement, and an address; the representation and permitted operations differ even when a debugger prints similar digits.

**References:** Patterson and Hennessy, *Computer Organization and Design*; Bryant and O'Hallaron, *Computer Systems: A Programmer's Perspective*.

### Prerequisite 2 - Locality, caches, and virtual memory

Temporal locality means recently used data may be reused; spatial locality means nearby addresses may be used. Caches move fixed-size lines and identify them with tags. Virtual memory divides addresses into pages; a TLB caches translations, while page tables define mappings and permissions. A cache miss and a page fault are radically different events. CPU/GPU managed memory adds placement and migration decisions on top of address translation. Work a 4 KiB-page example: offset bits select a byte within the page; remaining virtual bits identify the page and are translated before physical cache/DRAM access according to the implementation.

**References:** Bryant and O'Hallaron, *Computer Systems: A Programmer's Perspective*; Intel and AMD architecture manuals; Arm Architecture Reference Manual.

### Prerequisite 3 - Parallelism and synchronization

Instruction-level parallelism overlaps independent operations in one thread. Data parallelism applies one operation across elements. Thread parallelism runs independent instruction streams. Synchronization establishes ordering and visibility; it is not merely “waiting.” Atomics provide indivisible operations with specified ordering. A barrier normally coordinates a defined group, not every thread in a system. Draw a dependency graph before parallelizing: nodes are operations, edges are required order. The longest dependency chain limits latency even with unlimited workers.

**References:** Hennessy and Patterson, *Computer Architecture: A Quantitative Approach*; Herlihy and Shavit, *The Art of Multiprocessor Programming*.

### Prerequisite 4 - Performance measurement and uncertainty

Latency is time per operation; throughput is operations per time. Bandwidth is bytes per time. FLOPS counts floating-point operations under a convention and says nothing about correctness. Warm-up, clocks, power limits, NUMA placement, compiler transformations, caching, asynchronous APIs, and timer placement change results. Report distributions and configurations. A confidence interval cannot repair a biased benchmark. Validate output, synchronize at measurement boundaries, inspect generated code, and test multiple sizes.

**References:** Hennessy and Patterson, *Computer Architecture: A Quantitative Approach*; Lilja, *Measuring Computer Performance*.

### Prerequisite 5 - Floating point, vectors, and matrices

IEEE 754 floating point trades exactness for range. Addition is not associative, so parallel reductions can produce different low bits. SIMD/SIMT lanes perform related operations; matrix engines accelerate structured multiply-accumulate tiles at selected precisions. Peak “operations” often counts one multiply-add as two. Before comparing hardware, align data type, sparsity assumptions, accumulation type, error tolerance, and counting convention.

**References:** IEEE 754; Higham, *Accuracy and Stability of Numerical Algorithms*; NVIDIA, AMD, and Intel programming guides.

### Prerequisite 6 - Specifications versus measurements

An architecture specification defines permitted observable behavior. A vendor optimization guide describes expected implementation behavior but may be conditional. A whitepaper selects explanatory and marketing details. A research paper supplies a method and evidence with bounded validity. A microbenchmark infers hidden structure. Maintain separate columns for guaranteed, documented recommendation, reported product fact, and experimental inference. This prevents code from depending accidentally on a cache size, warp-synchronous behavior, or ordering rule that was never guaranteed.

**References:** AMD64 and Intel software developer manuals; Arm Architecture Reference Manual; CUDA, HIP, and oneAPI programming guides.
`;

function diagram(p){return `\`\`\`mermaid\nflowchart LR\n A["${p.flow[0]}"] --> B["${p.flow[1]}"] --> C["${p.flow[2]}"] --> D["${p.flow[3]}"] --> E["${p.flow[4]}"]\n E -. measurement .-> A\n\`\`\``;}
function render(p){
 const close=p.flow.map((x,i)=>`### Close-reading station ${i+1}: ${x}\n\nLocate how the source defines **${x}**. Record whether it is guaranteed by an ISA/programming specification, described for one microarchitecture, or inferred experimentally. Connect it to the adjacent stages in ${p.flow.join(" -> ")}. Then write one correctness test and one performance test. The correctness test must avoid timing assumptions; the performance test must record configuration and uncertainty.`).join("\n\n");
 return `# ${p.title}\n\n> **Primary sources:** ${p.sources}\n>\n> This chapter is a first-reading replacement, not an instruction-list replacement. Follow live specifications for normative details and use the local PDFs for stable study.\n\n## Paper and specification at a glance\n\n| Lens | Answer |\n|---|---|\n| Guiding question | ${p.question} |\n| Core model | ${p.core} |\n| Critical comparison | ${p.compare} |\n| Formal lens | ${p.formal} |\n| Practical lab | ${p.lab} |\n\n## Concept map\n\n${diagram(p)}\n\nThe diagram is a chain of contracts. Each arrow can preserve correctness while changing cost. Never infer a microarchitectural structure solely from an API name, and never infer performance solely from an ISA feature.\n\n## Tutorial 1 - Intuitive understanding\n\n+Think of ${p.analogy}. ${p.question}\n\n${p.core}\n\nWalk through ${p.flow.join(" -> ")}. At the first stage, software expresses work. Intermediate stages translate, schedule, store, or move it. The final stage is observable only through architectural results and measurements. A faster middle stage may not improve the whole path when data movement or serialization dominates.\n\n${p.compare}\n\nThree traps matter. First, “core” means different things across vendors. Second, shared address space does not imply shared physical memory or equal access time. Third, a headline peak assumes enough independent work, favorable types, and sustained data supply. The teach-back test is to explain the diagram without using “the GPU is just faster” or “Arm is low power.”\n\n## Tutorial 2 - Practitioner understanding\n\n${p.core}\n\n### From source to machine behavior\n\n${diagram(p)}\n\nAt every boundary record inputs, outputs, ownership, ordering, and cost. Compilers may vectorize, fuse, reorder, or eliminate work. Drivers and runtimes may queue asynchronously. Hardware may cache, speculate, coalesce, or migrate data while preserving its contract. Therefore profile the executed artifact, not the source code you intended.\n\n### Performance model\n\n${p.formal}\n\nUse units throughout. Convert bytes and operations consistently, distinguish decimal GB from binary GiB, and separate elapsed time from device-only time. Check numerical results after every optimization. For concurrent code, use language/runtime synchronization rather than a timing delay. For accelerator code, synchronize only where required, but include required synchronization in end-to-end measurements.\n\n### Build and measurement lab\n\n${p.lab}\n\nRequired artifact: source, compiler command, generated-code excerpt, hardware/driver inventory, raw timing rows, summary with uncertainty, correctness check, and a short explanation of one rejected hypothesis. Acceptance means another practitioner can reproduce the shape of the result and tell which claims are specification-backed versus inferred.\n\n## Tutorial 3 - Researcher understanding\n\n${p.formal}\n\n### Reading normative documentation\n\nRead “must,” “shall,” “may,” implementation-defined, and undefined literally. Identify the architecture/version and execution mode. Separate functional semantics from performance advice. Specifications often describe an abstract machine; implementations may reorder internally so long as prohibited observations never become visible. Programming models add another abstract machine whose compiler mapping must be justified.\n\n### Reconstructing evidence\n\n${p.compare}\n\nFor each reported number, record device, revision, clock/power state, operand types, instruction mix, working-set size, layout, compiler, repetitions, estimator, and whether transfers or launch are included. Reproduce a baseline before the claimed mechanism. Use dependent operations for latency and independent operations for throughput. Inspect machine code so compiler behavior is not the hidden treatment.\n\n### Threats to validity and extensions\n\nInternal threats include timer overhead, dead-code elimination, unreported throttling, cache warm state, asynchronous completion, occupancy differences, and counter multiplexing. External threats include a new generation, different driver/compiler, integrated versus discrete memory, virtualization, and workload structure. Construct an extension with competing hypotheses whose predicted curves differ; a single faster/slower number rarely identifies a mechanism.\n\n${close}\n\n${prerequisites}\n\n## Glossary\n\n- **ISA:** software-visible machine contract.\n- **Microarchitecture:** a hardware implementation of an ISA or execution model.\n- **ABI:** conventions allowing separately compiled software to interoperate.\n- **SIMD:** one instruction operates across explicit vector lanes.\n- **SIMT:** threads are programmed individually but scheduled in groups.\n- **Coherence:** rules keeping cached copies of a location consistent.\n- **Consistency model:** permitted ordering/visibility of memory operations.\n- **Occupancy:** resident parallel groups relative to an architectural maximum.\n- **Arithmetic intensity:** operations performed per byte moved.\n- **Roofline:** upper-bound model combining compute and bandwidth ceilings.\n\n## Checkpoint\n\n- **Intuition:** explain the analogy, the five-stage map, and two cases where a headline comparison fails.\n- **Practitioner:** complete the lab with correct results, full timing boundaries, generated-code inspection, and a reproducible environment.\n- **Researcher:** classify every claim as normative, documented, product-specific, or inferred; reconstruct one model; and design a controlled falsification experiment.\n`;
}
for(const p of profiles){let text=render(p);if(text.length<14000)text += `\n## Extended research workbook\n\n`+Array.from({length:8},(_,i)=>`### Investigation ${i+1}\n\nApply the chapter's formal model to ${p.flow[i%p.flow.length]}. State a paper- or specification-backed claim, an alternative explanation, the smallest discriminating experiment, controlled variables, expected observations under both hypotheses, and the boundary beyond which the conclusion must not be generalized. Relate the result to: ${p.formal}`).join("\n\n"); if(text.length<14000)throw new Error(`${p.file}: ${text.length}`);const out=path.join(root,p.file);fs.mkdirSync(path.dirname(out),{recursive:true});fs.writeFileSync(out,text);console.log(`${p.file}: ${text.length}`);}

